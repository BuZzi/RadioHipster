<IfModule mod_rewrite.c>
    Options -MultiViews

    RewriteEngine On
    #Lien vers l'application
    RewriteBase /RadioHipster/web
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteRule ^ index.php [L]
</IfModule>